import java.util.ArrayList;
import java.util.Iterator;

public class Main {

    public static <arraylist, arraylist> void main(String[] arraylist) {
        arraylist card = new arraylist<String>();
    }
    public static void main(String[] arraylist) {
        arraylist<String> deck = new arraylist<String>();
        String[] Palo = {"Treboles", "Corazones", "picas", "Diamantes"};
        String[] Color = {"Rojo", "Negro"};
        String[] Valor = {"2", "3", "4", "5", "6", "7", "8", "9", "10", "A", "J", "Q", "K",};

    }

    Public station void shuffle (ArrayList deck){
        ArrayList<String> values = deck;
        Collections.shuffle(calues);
        System.out.println("\n se mezcla el deck");
        Iterator<string> = nombreIterator2.next();
        while (nombre Iterator2.has next());
        system.out.print(elemento+"\")
    }

    Public station void head (ArrayList deck) {
        System.out.println(deck.get(0));
        deck.remove (0);
        system.out.println ("qudan" + deck.size +  "cartas");
    }

    Public station void pick (ArrayList deck) {
        System.out.println(deck.get(aleatorio.nextIn(decksize)));
        deck.remove (aleatorio.nextInt(deck.size()));
        system.out.println ("qudan" + deck.size +  "cartas");
    }

    Public station void hand (ArrayList deck) {
        for (int i=0, i=4; i++){
            system.out.println(deck.get(i));
        }
        for (int i=0, i=4; i++){
            deck.remove(i);
        }
        System.out.println("qudan" + deck.size +  "cartas");
    }

}
